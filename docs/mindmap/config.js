(function() {
    window.KITYMINDER_CONFIG = {
        readOnly: false,

        // default template
        defaultTemplate: 'default',

        defaultTheme: 'fresh-blue',

        maxUndoCount: 20,

        lang: 'zh-cn',
        
        // image size limit
        maxImageWidth: 200,
        maxImageHeight: 200,

        // save interval
        autoSave: 2
    };
})();